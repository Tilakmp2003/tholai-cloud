version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - cd backend
      - npm ci
      - npx prisma generate
      - npm run build
run:
  command: node backend/dist/server.js
  network:
    port: 8080
