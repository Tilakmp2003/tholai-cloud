{
  "name": "vsc-lambda-agent",
  "version": "1.0.0",
  "description": "Lambda agent runner for VSC",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --target=node18 --outfile=dist/index.js --external:aws-sdk --external:ssh2 --external:cpu-features"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.5.0",
    "esbuild": "^0.19.2",
    "typescript": "^5.1.6"
  }
}
